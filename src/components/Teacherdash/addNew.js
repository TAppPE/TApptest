import React from 'react';
import './addNew.css';

const AddNew = () => {
    return(
       <div id="addNewContainer">
           <div id="makeRows">
           <button id="addNewBtn">Add New Class +</button>
           </div>
       </div>
    );
};

export default AddNew;