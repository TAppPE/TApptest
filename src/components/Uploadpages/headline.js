import React from 'react';
import './headline.css';

const headline = () => {
    return (
       <div id="central">
           <div id="centers"><h1 id="exheadline">Watch the example footage to see what the examiner is looking for. Then click the upload/ record button to submit your evidence.</h1>
       </div>
       </div>
    ) 
};

export default headline;